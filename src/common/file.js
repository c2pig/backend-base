'use strict';
class File {
  constructor(file) {
    this.file = file;
  }
  write(content, path, callback) {
    //this.file.publish({ Message: msg, TopicArn: topic }, callback);
  }
}
module.exports = File;
